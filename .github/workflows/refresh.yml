name: Refresh match data (retired)

on:
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "KickOff Oracle is retired; data refresh is disabled."
